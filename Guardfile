guard 'livereload', notify: true do
  # File types LiveReload may optimize refresh for
  DOCROOT = 'public_'
  watch(%r{.+\.(css|js)\Z})

  # Files needing a full reload of the page
  watch(%r{.+\.(php)\Z})
end
